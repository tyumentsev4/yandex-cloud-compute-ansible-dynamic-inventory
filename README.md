# Yandex Cloud Compute Cloud Ansible dynamic inventory

Groups Yandex Cloud Compute Cloud hosts based on the `ansible_group` label.

## Environment variables

```bash
export YC_TOKEN=$(yc iam create-token)
export YC_FOLDER_ID=$(yc config get folder-id)
```
